import AllEvents from "../components/AllEvents";
import FilterEvents from "../components/FilterEvents";

const Home = () => {
  return (
    <section>
      <FilterEvents />
      <AllEvents />
    </section>
  );
};

export default Home;
